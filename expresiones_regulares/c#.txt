~ para definir variables en c# tenemos que especificar el tipo de dato por ej:

string nombre = "NombreDetuMadre";
int edad = 20;
bool ver = 1<10; {esto tendria que devolver true}
double estatura = 20.1

~ podemos convertir un string a int por ejemplo


string numero_ejemplo = "20";
int numero_ejemplo = int32.Parse(numero_ejemplo);

~ podemos convertir int a string

int numero_ejemplo = 20;
string numero_ejemplo = numero.ToString();


~ la sintaxis de las condicionales es la siguiente:

int numero_uno = 100;
int numero_dos = 200;

if (numero_uno < numero_dos)
{
	MessageBox.show(numero_uno + " es menor que " + numero_dos)		
}
else
{
	MessageBox.show(numero_uno + " es mayor que " + numero_dos)
}

~ bucle while

while (numero < 10)
{
	numero++; // esto hace que el incremente el numero
	textlbl.Text = numero
}





~ comentarios 


// con el doble slash podemos comentar linea

/*
esto comentara todo
este trozo

*/



~ sintaxis de funcion


public void NombreDeFuncion(parametro)
{
	// codigo aqui
}